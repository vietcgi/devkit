---
galaxy_info:
  author: "Kevin Vu"
  description: "Manage git configuration, templates, hooks, and aliases"
  company: "DevKit"
  license: "MIT"
  min_ansible_version: "2.9"

  platforms:
    - name: macOS
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all

  galaxy_tags:
    - git
    - vcs
    - configuration
    - hooks
    - development

dependencies:
  - role: core
    tags: [always]
